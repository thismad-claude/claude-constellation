services:
  constellation:
    build: .
    container_name: claude-constellation
    network_mode: host
    volumes:
      # Modify this path to your .claude directory
      - ${HOME}/.claude:/root/.claude:ro
    environment:
      - HOME=/root
    restart: unless-stopped
